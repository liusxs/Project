// 蛇对象
class Snake{
    constructor(select){
        this.map=document.querySelector(select)
        // 蛇的运动方向
        this.direction="right"
        // 蛇的数组(把蛇的头和身体都会储存到数组中，头从数组的第0位开始)
        this.snakelist=[]
    }
    // 创建蛇头的函数
    createHead(){
        // 拿到蛇头
        
    }
}